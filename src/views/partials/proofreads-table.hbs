{{#if proofreads.length}}
<div class="bg-white rounded-xl border border-gray-200 overflow-hidden">
  <table class="min-w-full divide-y divide-gray-200">
    <thead class="bg-gray-50">
      <tr>
        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Video</th>
        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Sprache</th>
        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Status</th>
        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Rev.</th>
        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Aktionen</th>
      </tr>
    </thead>
    <tbody class="divide-y divide-gray-200">
      {{#each proofreads}}
      <tr class="hover:bg-gray-50">
        <td class="px-4 py-3 text-sm font-medium text-gray-900">{{this.video_title}}</td>
        <td class="px-4 py-3 text-sm text-gray-600">{{this.language}}</td>
        <td class="px-4 py-3">{{{statusBadge this.status}}}</td>
        <td class="px-4 py-3 text-sm text-gray-500">
          {{#if this.excel_revision}}v{{this.excel_revision}}{{else}}–{{/if}}
        </td>
        <td class="px-4 py-3">
          <div class="flex gap-2">
            {{#if (eq this.status "completed")}}
            <a href="/api/proofreads/{{this.id}}/excel" class="text-xs text-blue-600 hover:underline">⬇ Excel</a>
            <button hx-get="/projects/{{this.project_id}}/proofreads/{{this.id}}/upload" hx-target="#modal" class="text-xs text-green-600 hover:underline">⬆ Upload</button>
            {{/if}}
            {{#if (eq this.status "edited")}}
            <a href="/api/proofreads/{{this.id}}/excel" class="text-xs text-blue-600 hover:underline">⬇ Excel</a>
            <button hx-get="/projects/{{this.project_id}}/proofreads/{{this.id}}/upload" hx-target="#modal" class="text-xs text-green-600 hover:underline">⬆ Re-Upload</button>
            <span class="text-xs text-yellow-600">✓ Editiert</span>
            {{/if}}
            {{#if (eq this.status "done")}}
            <span class="text-xs text-emerald-600">✓ Fertig</span>
            {{/if}}
            {{#if (eq this.status "failed")}}
            <span class="text-xs text-red-600" title="{{this.error_message}}">✗ Fehler</span>
            <button hx-post="/api/proofreads/{{this.id}}/retry" hx-swap="none" class="text-xs text-blue-600 hover:underline">↻ Retry</button>
            {{/if}}
            {{#if (eq this.status "processing")}}
            <span class="text-xs text-gray-400">⏳ Verarbeitung...</span>
            {{/if}}
            {{#if (eq this.status "generating")}}
            <span class="text-xs text-purple-500">⏳ Video wird erzeugt...</span>
            {{/if}}
          </div>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
</div>
{{else}}
<div class="text-center py-12 bg-white rounded-xl border border-gray-200">
  <p class="text-gray-500">Noch keine Proofreads. Erst Videos hinzufügen, dann "Proofreads generieren" klicken.</p>
</div>
{{/if}}
